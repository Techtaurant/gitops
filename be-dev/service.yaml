apiVersion: v1
kind: Service
metadata:
  name: techtaurant-be
  labels:
    app.kubernetes.io/name: techtaurant-be
    app.kubernetes.io/instance: be-dev
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: techtaurant-be
    app.kubernetes.io/instance: be-dev
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
