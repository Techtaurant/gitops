apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-be-dev-to-redis
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: redis
      app.kubernetes.io/instance: redis-dev
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: be-dev
      ports:
        - protocol: TCP
          port: 6379
