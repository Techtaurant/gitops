apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: redis-dev
resources:
  - service.yaml
  - statefulset.yaml
  - network-policy.yaml
  # Add secret.yaml here after generating a SealedSecret with kubeseal.
